# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(port_driven_timeoutTaskLib)
ADD_LIBRARY(${PORT_DRIVEN_TIMEOUT_TASKLIB_NAME} SHARED
    ${PORT_DRIVEN_TIMEOUT_TASKLIB_SOURCES})
add_dependencies(${PORT_DRIVEN_TIMEOUT_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${PORT_DRIVEN_TIMEOUT_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${PORT_DRIVEN_TIMEOUT_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${PORT_DRIVEN_TIMEOUT_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${PORT_DRIVEN_TIMEOUT_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${PORT_DRIVEN_TIMEOUT_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${PORT_DRIVEN_TIMEOUT_TASKLIB_INTERFACE_LIBRARIES}")


INSTALL(TARGETS ${PORT_DRIVEN_TIMEOUT_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${PORT_DRIVEN_TIMEOUT_TASKLIB_HEADERS}
    DESTINATION include/orocos/port_driven_timeout)

